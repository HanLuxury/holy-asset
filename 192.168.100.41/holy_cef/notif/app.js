(function(){const stack=document.getElementById('stack');function parse(d){if(typeof d==='string'){try{return JSON.parse(d||'{}')}catch(e){return {}}}return d||{}}window.Cef=window.Cef||{_handlers:{},on(e,fn){this._handlers[e]=fn},_trigger(e,json){if(this._handlers[e])this._handlers[e](parse(json));}};function areas(){try{const rects=[...document.querySelectorAll('.notif')].map(el=>{const r=el.getBoundingClientRect();return[r.left,r.top,r.width,r.height]});CefBridge.updateInteractiveAreas(JSON.stringify(rects))}catch(e){}}function esc(v){return String(v??'').replace(/[&<>"]/g,s=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;'}[s]))}function show(data){data=parse(data);const time=Math.max(1200,Math.min(15000,Number(data.time||4500)));const el=document.createElement('div');el.className='notif '+(data.type||'info');el.innerHTML=`<div class="title">${esc(data.title||'NOTIFICATION')}</div><div class="msg">${esc(data.message||'')}</div><div class="bar"></div>`;stack.prepend(el);el.querySelector('.bar').animate([{transform:'scaleX(1)'},{transform:'scaleX(0)'}],{duration:time,fill:'forwards'});setTimeout(()=>{el.remove();areas()},time+120);setTimeout(areas,20)}Cef.on('notif:show',show);Cef.on('cef:clear',()=>{stack.innerHTML='';areas()});document.addEventListener('DOMContentLoaded',()=>setTimeout(()=>{try{CefBridge.cefReady()}catch(e){}areas()},80));})();
