# Phone UI skin

The phone UI uses a FiveM-inspired modern RP smartphone look: glass cards, floating hardware frame, app-grid launcher, status bar, bottom dock, blur, and PNG application icons.

All gameplay/data logic remains in `pawno/include/modules/phone.inc`; this folder only changes the CEF presentation and local PNG assets.
